<h1>Cart page</h1>
{% for item in cart.items %}
    <div>
        <p>Item is {{ item.title }}</p>
        <p>Price is {{ item.price |  money_with_currency }}</p>
        <img src="{{item.image |  image_url: width: 400 }}" alt="">
    </div>
{% endfor %}